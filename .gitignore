node_modules/
.astro/
.DS_Store
*.log
.env
.env.*

# This project uses pnpm; keep other lockfiles out
package-lock.json
yarn.lock

# dist/ is committed so Hostinger Git can publish the static site
# without a Node build step on the server
